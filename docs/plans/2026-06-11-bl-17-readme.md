# Plan: BL-17 README User Documentation

**Date:** 2026-06-11
**Backlog item:** BL-17
**Status:** In progress

## Goal

Rewrite `README.md` to be user-facing — what Mintangle does, how to install it,
the full shortcut table, repeat-cycling explanation, and settings. Keep the
existing dev section but push it later in the document.

## README Structure

1. **Title + 1-line overview** — remove "early development / no-op skeleton"
   status note (the extension is now functional)
2. **Installation** — enable via Cinnamon → System Settings → Extensions, plus
   the `npm run install:local` method for those building from source
3. **Default shortcuts table** — all 26 shortcuts from PRODUCT.md exactly
4. **Repeat cycling** — brief prose: position-only cycling per category (halves,
   corners, thirds, fourths, three-fourths, two-thirds), sizes do not cycle.
   Each category gets 1 sentence on what it cycles through. Point to PRODUCT.md
   for full detail.
5. **Settings** — table of the 7 user-configurable settings with types and defaults
6. **Development** — existing section, largely unchanged; update project layout
7. **Documentation links** — PRODUCT.md, AGENTS.md, docs/backlog

## Constraints

- Shortcut table must match PRODUCT.md exactly (26 actions)
- Repeat cycling must match actual implemented behavior
- No product spec duplication — reference PRODUCT.md for exhaustive detail
- No dev/agent notes in user-facing sections

## Files Changed

- `README.md` — rewritten
- `docs/backlog/done/BL-17-readme.md` — archived on completion

## Deviations

None.
